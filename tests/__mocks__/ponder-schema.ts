// Stub for ponder:schema virtual module — only used to satisfy imports in vitest.
// Values are placeholders; tests that exercise ponder schema columns must mock them.
export const conditionalOrderGenerator = {} as never;
export const discreteOrder = {} as never;
